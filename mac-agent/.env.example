# Cloud app URL (no trailing slash)
CLOUD_URL=https://your-app.railway.app

# Must match AGENT_SECRET in cloud app .env
AGENT_SECRET=change-me-to-random-hex-string

# Optional: path for log file (default: ./agent.log)
# LOG_FILE=/Users/ben/morning-system/mac-agent/agent.log
