# ── Anthropic ──────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=sk-ant-...

# ── Database ────────────────────────────────────────────────────────────────
# Railway PostgreSQL add-on sets this automatically.
# For local dev, use a local PostgreSQL instance:
#   postgresql://user:password@localhost:5432/morning_system
DATABASE_URL=postgresql://user:password@localhost:5432/morning_system

# ── Google OAuth ───────────────────────────────────────────────────────────
# Create at: https://console.cloud.google.com/apis/credentials
# Scopes needed: Gmail API (readonly), Google Calendar API (readonly), userinfo.email
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret

# The redirect URI registered in Google Cloud Console
# For local dev: http://localhost:3000/auth/google/callback
# For production: https://your-app.railway.app/auth/google/callback
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# ── Session ────────────────────────────────────────────────────────────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=change-me-to-random-hex-string

# ── Mac Agent Auth ─────────────────────────────────────────────────────────
# Must match AGENT_SECRET in mac-agent/.env
# Generate with: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
AGENT_SECRET=change-me-to-random-hex-string

# ── Server ─────────────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=development

# Optional: CORS allowed origin (default: all)
# ALLOWED_ORIGIN=https://your-app.railway.app
